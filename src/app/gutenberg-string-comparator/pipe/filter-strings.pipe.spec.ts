import { FilterStringsPipe } from './filter-strings.pipe';

describe('FilterStringsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStringsPipe();
    expect(pipe).toBeTruthy();
  });
});
